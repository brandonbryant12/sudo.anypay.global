{{!-- app/templates/application.hbs --}}

<div class="container">
  <div class="menu">

    {{#if session.isAuthenticated}}
      {{#link-to action "invalidateSession"}}>Logout{{/link-to}}
    {{else}}
      {{#link-to "login"}}Login{{/link-to}}
    {{/if}}
  </div>
  <div class="main">
    {{outlet}}
  </div>
</div>

